package decorator

// Decorator
// Adding behaviour without modifying the type itself
// Not rewriting or altering existent code (Open-close principle)
// Keep new functionality separate (single responsibility principle)
// Through embedding
